export enum AppPagePaths {
    HOME = '/',
    // PROTECTED = '/protected',
}

export enum AppThemes {
    LIGHT = 'light',
    DARK = 'dark',
}

export enum SupportedChains {
    ETH = 1,
    ARBITRUM = 42161,
    BASE = 8453,
    GNOSIS = 100,
}
